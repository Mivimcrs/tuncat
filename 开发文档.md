# TunCat 开发文档（v0.1 草案）

> 暂定名 **TunCat**（托盘图标是猫，做 TUN 修复）。本文件是开发前设计稿，等待确认后开工。
> 日期：2026-09-03

---

## 0. 待确认决策点（开工前必须拍板）

| # | 决策项 | 推荐 | 备选 | 理由 |
|---|--------|------|------|------|
| D1 | 软件名称 | **TunCat** | TUN Doctor / 自定义 | 猫图标 + TUN 修复，好记好搜 |
| D2 | GUI 框架 | **egui/eframe（纯 Rust）** | Tauri 2（WebView） | egui 单二进制、无 Node 工具链、深浅主题一行切换；Tauri 界面更华丽但要前端栈，CI 更重。本工具界面简单，egui 够用 |
| D3 | 开源协议 | **MIT** | GPL-3.0 | 工具类小软件 MIT 传播阻力最小 |
| D4 | 修复方向默认值 | **物理网卡 → TUN**（脚本 1 方向） | TUN → WLAN（脚本 2 方向） | 改 TUN 的 IP 比改 WLAN 安全，WLAN 被改 192.168.137.1 期间会断开路由器。做成可配置，两个方向都保留 |
| D5 | 探测地址默认值 | `http://www.gstatic.com/generate_204`（可配置） | `http://cp.cloudflare.com/generate_204` / 自定义 | 走 TUN 时经代理可达；TUN 坏了直连也不通，判定准确。前提条件里有"Clash 在跑"兜底，不会误判 |
| D6 | README 语言 | 中文为主 + 英文简版 | 纯中文 | 国内用户主要受众是中文 |

---

## 1. 背景：问题原理

**现象**：安装 OPPO 互联（OPPO Connect / 跨设备互联服务）后，Clash/Mihomo 的 TUN 模式无法联网。

**原因**（机制层面）：Mihomo 的 TUN 模式使用 Wintun 虚拟网卡接管系统流量。OPPO 互联安装了自己的网络过滤驱动/组件，干扰了 Windows 对新建虚拟网卡的初始化流程——网卡虽然创建成功（状态 Up），但 Windows 的 NLA（网络位置感知）没有完成识别，路由 metric、DNS 解析链路不完整，导致流量进了 TUN 却出不去。

**解法（本软件的核心，来自已验证的脚本方案）**：做一次 **ICS 脉冲**——

1. 清除当前所有 ICS 共享；
2. TUN 网卡设为 private（被共享端），物理网卡设为 public（共享源）；
   - 注意顺序：**必须先 private 后 public**，反了报 `0x80004005`；
3. 保持 2 秒。这一步会强制 Windows 走完整网卡初始化：拉起 SharedAccess 服务、给 private 端分配 192.168.137.1/24、写 NAT/路由/NLA 识别；
4. 关闭所有共享（含 `finally` 兜底），网卡已"登记在册"，Mihomo 重新接管后恢复正常。

参考实现：脚本 1（PowerShell 手动版，逻辑健壮，本软件照此思路重写）与脚本 2（bat 定时版，有多个 bug，只借鉴日志和计划任务思路）。

## 2. 目标与非目标

**目标**
1. Rust 实现 ICS 脉冲修复，开机后台静默自启（无窗口、无 UAC 弹窗）。
2. 周期性检测 TUN 健康状态，异常时自动修复，全程无需人工干预。
3. 提供手动操作界面：立即检测、立即修复、日志查看、参数设置。
4. 关闭窗口最小化到托盘，常驻后台。
5. 深色/浅色主题（可跟随系统）。
6. 托盘图标：纯色背景 + 线条猫咪，颜色随状态变化。
7. 开源托管 GitHub，CI 自动出 Windows release。

**非目标（明确不做）**
- 不做 Clash 配置管理、不内置代理内核；
- 不做流量统计/规则管理（和 Clash 客户端重复）;
- 不跨平台（ICS 是 Windows 独有机制，只支持 Windows 10/11 x64）；
- 不处理"Clash 没开 TUN"的场景（那是用户自己的选择）。

## 3. 技术选型

| 领域 | 选型 | 说明 |
|---|---|---|
| 语言 | Rust（stable，2024 edition） | |
| GUI | `eframe`/`egui` | 即时模式，深浅主题内置切换，`Context::set_visuals` |
| 托盘 | `tray-icon` + `muda`（菜单） | 与 egui 配合的标准方案，事件走主线程 event loop proxy |
| COM/ICS | `windows` crate，feature `Win32_NetworkManagement_InternetConnectionSharing` | windows-rs 自带 `INetSharingManager` 等接口，Rust 原生调 COM，不再起 PowerShell 子进程 |
| 网卡枚举 | `windows` crate，feature `Win32_NetworkManagement_IpHelper` | `GetAdaptersAddresses` 取名称/状态/IP，替代 `Get-NetAdapter` |
| 健康探测 | `ureq`（阻塞式 HTTP，带超时） | 无需 tokio，后台线程里跑 |
| 配置 | `serde` + `serde_json` | `%APPDATA%\TunCat\config.json` |
| 日志 | `tracing` + `tracing-appender` | 滚动文件日志，`%APPDATA%\TunCat\logs\`，保留 7 天 |
| 开机自启 | `schtasks` 封装（计划任务 /RL HIGHEST /SC ONLOGON） | 注册表 Run 键无法提权；计划任务以最高权限静默运行，开机无 UAC 弹窗 |
| 单实例 | `windows` crate 命名互斥体 | 防止双开 |
| 错误处理 | `anyhow` + `thiserror` | |
| 构建/CI | cargo + GitHub Actions（windows-latest，release 出 zip + 固定 ico） | |

**提权策略**（关键设计）：
- 程序清单（manifest）声明 `requireAdministrator`：手动双击启动时弹一次 UAC，保证 ICS 操作永远有权限；
- 开机自启用计划任务（最高权限运行），登录时静默拉起，**无 UAC 弹窗、无窗口**，只驻留托盘；
- 若被非管理员上下文意外拉起（如手动从 explorer 以降权方式运行），ICS 调用会失败——UI 顶部显示红色横幅"需要管理员权限"，提供"以管理员重启"按钮。

## 4. 系统架构

```
┌─────────────────────────────────────────────┐
│                 tuncat (bin)                 │
│  ┌──────────────┐      ┌─────────────────┐  │
│  │  UI (eframe)  │◄────│  Tray (tray-icon)│  │
│  │  主窗口/设置   │      │  菜单/单击显隐    │  │
│  └──────┬───────┘      └────────┬────────┘  │
│         │ mpsc channel           │           │
│  ┌──────▼────────────────────────▼───────┐  │
│  │           Core Worker 线程              │  │
│  │  状态机：检测 → 判定 → 修复 → 冷却        │  │
│  └──────┬──────────────┬─────────────────┘  │
│         │              │                    │
│  ┌──────▼─────┐  ┌─────▼──────────────┐     │
│  │ Detector    │  │ IcsPulser (COM)    │     │
│  │ 网卡枚举     │  │ EnumEveryConnection │     │
│  │ 健康探测     │  │ Enable/DisableShare │     │
│  └────────────┘  └────────────────────┘     │
└─────────────────────────────────────────────┘
```

- **workspace 双 crate**：`tuncat-core`（纯逻辑库，可单测）+ `tuncat`（bin：UI + 托盘 + 组装）。核心逻辑不依赖 GUI，保证可测试。
- UI 与 Core 之间用 `std::sync::mpsc` 双向通道：UI 发命令（立即检测/修复/改配置），Core 推状态快照和日志事件。
- UI 只是 Core 的一个"订阅者"——Core 不依赖窗口是否存在，托盘常驻即可工作。

## 5. 核心逻辑设计

### 5.1 检测流程（每个检测周期执行）

```
前置条件（不满足 → 状态=跳过，等下个周期）
  ├─ TUN 适配器存在？名称匹配关键词列表（默认: mihomo, Meta Tunnel, wintun, vgate0，可配置）
  └─ TUN 适配器状态 == Up？

健康探测（满足前置条件才做）
  └─ HTTP GET 探测地址，超时 3 秒（可配置）
      ├─ 2xx/204 → 健康
      └─ 超时/连接失败/非 2xx → 不健康

判定
  ├─ 健康 → 重置连续失败计数
  └─ 不健康 → 计数 +1；达到阈值（默认 2 次）→ 触发修复
```

细节：
- 探测走系统默认路由。TUN 正常时流量经 Clash 出去；TUN 坏时直连也到不了 gstatic（被墙），正确判坏。若用户把探测地址改成国内直连可达的地址，TUN 坏但直连通 → 会误判健康，文档里写清这个权衡。
- DNS 失败与 TCP 失败分开记录到日志，方便区分"DNS 没走 TUN"和"链路不通"两类症状。

### 5.2 修复流程（ICS 脉冲）

```
1. 快照当前所有连接的共享状态（哪些开了、什么类型）   ← 脚本1没做，我们补上
2. DisableSharing 全部已开启的共享，逐个间隔 500ms
3. 重新枚举连接（COM 配置对象失效，必须重取）
4. TUN 网卡 EnableSharing(1)   ← private，先
5. 800ms 后，物理网卡 EnableSharing(0)   ← public，后
6. 保持 HoldSeconds（默认 2s）
7. finally 语义：重新枚举 → 全部 DisableSharing
8. 恢复步骤 1 快照的共享状态（用户原有的 ICS 不丢）
9. 冷却 WaitSeconds（默认 15s，给 Mihomo 重新接管的时间）后再恢复检测
```

物理网卡选择：优先"持有默认路由且有网关的活动网卡"，多个时让用户在设置里固定关键词（默认取第一个活动的）。方向可配置（见 D4）。

### 5.3 状态机

```
[BootDelay] --延迟 N 秒--> [Detect] --健康--> [Idle] --间隔--> [Detect]
                              |
                              └--不健康且计数≥阈值--> [Pulse] --> [Cooldown] --> [Detect]
任意状态失败异常 --> [Error]（托盘变红，自动修复暂停，等待下个周期重试）
连续修复 M 次（默认 3）仍不健康 --> [GiveUp]（托盘红色感叹号，只告警不再修复，用户手动干预或状态转好后自动恢复）
```

状态实时反映到托盘图标颜色和主界面状态卡片。

### 5.4 手动操作

- 「立即检测」：向 Core 发命令，立即跑一轮检测；
- 「立即修复」：无视计数阈值直接执行脉冲（同样走快照/恢复逻辑）；
- 「暂停自动修复 / 恢复」：托盘菜单和主界面都有入口。

## 6. 配置文件

`%APPDATA%\TunCat\config.json`，UI 修改即保存，Core 热加载：

```jsonc
{
  "autostart": true,              // 开机自启（写/删计划任务）
  "autostart_delay_sec": 10,      // 登录后延迟多少秒开始首次检测
  "check_interval_sec": 30,       // 检测间隔
  "probe_url": "http://www.gstatic.com/generate_204",
  "probe_timeout_sec": 3,
  "fail_threshold": 2,            // 连续失败几次触发修复
  "pulse_hold_sec": 2,            // ICS 保持时长
  "cooldown_sec": 15,             // 修复后冷却
  "max_consecutive_fixes": 3,     // 连续修复几次放弃
  "tun_keywords": ["mihomo", "Meta Tunnel", "wintun", "vgate0"],
  "public_keywords": ["以太网", "Ethernet", "WLAN"],  // 多网卡时的优先级从左到右
  "pulse_direction": "public_to_tun",  // public_to_tun（推荐）| tun_to_public
  "restore_sharing": true,        // 脉冲后恢复用户原有共享
  "theme": "system",              // system | light | dark
  "close_to_tray": true           // 点 X 是最小化到托盘还是退出
}
```

## 7. UI 设计

**单窗口三页签：状态 / 日志 / 设置。**

- **状态页**：
  - 大状态卡片：当前状态（健康/修复中/异常/已放弃）+ TUN 网卡名 + 探测延迟 ms；
  - 统计行：本次开机以来检测次数、修复次数、上次修复时间；
  - 两个主按钮：「立即检测」「立即修复」；自动修复暂停开关。
- **日志页**：滚动只读日志（Core 推送），带级别着色，「打开日志目录」按钮。
- **设置页**：第 6 节全部字段分组呈现（启动 / 检测 / 修复 / 高级），改完即时生效；「开机自启」开关切换时创建/删除计划任务。

**主题**：浅色/深色/跟随系统三档。egui 的 `Visuals::light()/dark()` 为基线，再套品牌色（猫的线条色）。切换即时生效并持久化。

**关闭行为**：默认点 X 最小化到托盘（气泡提示一次"程序仍在后台运行"）；`close_to_tray=false` 时点 X 直接退出（退出前确认对话框）。

## 8. 托盘设计

- **图标**：圆角方块纯色背景 + 白色线条猫咪（简笔勾勒，坐姿，尾巴卷起）。三态配色：
  - 正常 = 纯色绿背景（#2FA46A 系）
  - 修复中 = 纯色琥珀背景（#E8A33D 系）
  - 异常/已放弃 = 纯色红背景（#D25446 系）
  - 暂停自动修复 = 纯色灰背景
  同一 SVG 猫线条模板 + 换背景色生成四张图，嵌入二进制。
- **交互**：
  - 左键单击：显示/隐藏主窗口；
  - 右键菜单：显示主界面 / 立即检测 / 立即修复 / 暂停自动修复 / 退出；
  - 修复发生时气泡通知一次（可关）。

## 9. 开机自启实现

- 创建：`schtasks /Create /TN "TunCat" /TR "<exe 路径> --silent" /SC ONLOGON /RL HIGHEST /F`；
- `--silent` 启动参数：跳过主窗口，直接驻托盘；
- 删除：`schtasks /Delete /TN "TunCat" /F`；
- 设置页开关直接调上面两条；程序更新 exe 路径变化时（开发期）由用户重新开关一次，release 用户无感；
- 备选实现（如果 schtasks 封装有坑）：直接用 `windows` crate 的 Task Scheduler COM API（ITaskService），文档先记着，不阻塞主线。

## 10. 日志

- `tracing` 分级：INFO 常规（每次检测结论、每次修复步骤）、WARN（探测失败）、ERROR（COM 调用失败、放弃修复）；
- 滚动文件：每日一个 `tuncat.2026-09-03.log`，保留 7 天自动清理；
- UI 日志页显示最近 500 条内存环形缓冲，与文件日志同源。

## 11. 开源与发布

- 仓库：GitHub，公开，名称 = 软件名（如 `tuncat`）；
- 协议：MIT（待 D3 确认）；
- README：问题背景（OPPO 互联 + TUN 冲突原理图）、使用截图、下载安装、配置说明、构建方法、致谢（原 PowerShell 脚本思路）；
- CI（GitHub Actions）：
  - push/PR：`cargo fmt --check` + `cargo clippy -D warnings` + `cargo test`（windows-latest）；
  - tag `v*`：release 构建，产物 zip（exe + README + LICENSE），附带 SHA256；
- 发布节奏：v0.1.0 首个可用版，后续按反馈迭代；
- 图标源文件（SVG）进仓库 `assets/` 目录。

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| `windows` crate 的 ICS COM 接口与脚本行为有差异 | Phase 3 先做最小验证程序：Rust 调 `INetSharingManager` 枚举+开关共享，与 PowerShell 行为比对通过后才继续 |
| 脉冲期间 TUN 被改 192.168.137.1，2 秒内断网 | 属预期行为，日志和文档写明；HoldSeconds 可调小到 1s |
| 用户机器原有 ICS 共享被清掉 | 快照/恢复逻辑（5.2 步骤 1/8）；`restore_sharing` 可关 |
| OPPO 互联升级后偏方失效 | 无技术兜底，README 声明适用版本，失效时程序会进入 GiveUp 状态明确告警，而不是无限循环修复 |
| 计划任务被安全软件拦截 | 设置页提供状态检测（任务是否存在/是否最高权限），失败时提示手动方案 |
| egui 托盘在 Windows 上的事件循环集成坑 | Phase 6 首日先做托盘空壳 demo（图标+菜单+显隐），跑通再接 Core |

## 13. 项目结构

```
tuncat/
├── Cargo.toml              # workspace
├── crates/
│   ├── tuncat-core/        # 纯逻辑：检测、脉冲、状态机、配置、日志
│   │   ├── src/
│   │   │   ├── detector.rs     # 网卡枚举 + 健康探测
│   │   │   ├── ics.rs          # COM ICS 脉冲（快照/开关/恢复）
│   │   │   ├── state.rs        # 状态机 + worker 线程
│   │   │   ├── config.rs
│   │   │   └── autostart.rs    # schtasks 封装
│   │   └── tests/              # 状态机与配置的单测（COM 部分手动集成测）
│   └── tuncat/             # bin：egui UI + 托盘 + main 组装
│       ├── src/
│       │   ├── main.rs
│       │   ├── ui/（status.rs / logs.rs / settings.rs / theme.rs）
│       │   └── tray.rs
│       ├── assets/tray_*.svg → 编译期嵌入
│       └── tuncat.rc + manifest（requireAdministrator + 图标）
├── assets/                 # 图标源文件
├── .github/workflows/      # ci.yml + release.yml
├── README.md
└── LICENSE
```

## 14. 行动清单（确认后按序执行）

| 阶段 | 内容 | 产出 | 验收 |
|---|---|---|---|
| P0 | 决策点 D1–D6 拍板 | 本文档定稿 | 用户确认 |
| P1 | workspace 骨架 + CI + README 空架 | 可编译的空项目 + GitHub 仓库 | push 后 CI 绿 |
| P2 | Detector：网卡枚举（关键词匹配 TUN/物理网卡）+ 健康探测 | core 模块 + 单测 | 枚举结果与 `Get-NetAdapter` 一致；探测有正确超时 |
| P3 | IcsPulser：Rust COM 最小验证 → 完整脉冲（快照/清空/开/关/恢复） | core 模块 + 手动集成测试脚本 | 在装有 Mihomo 的真机上，脉冲后 TUN 恢复联网 |
| P4 | 状态机 worker + 配置 + 日志 | core 完整 | 单测覆盖状态迁移；配置热加载 |
| P5 | egui UI 三页签 + 深浅主题 | bin 界面 | 手动检测/修复按钮工作；主题切换即时生效 |
| P6 | 托盘：图标三态 + 菜单 + 显隐 + 关闭最小化 + 单实例 | 常驻体验完整 | 点 X 进托盘；单击唤出；退出干净（无残留进程） |
| P7 | 开机自启（schtasks）+ `--silent` 静默启动 | autostart 模块 | 重开机验证：无窗口无 UAC，托盘自动出现并按时开始检测 |
| P8 | 猫咪托盘/应用图标（SVG 源 + 多分辨率 ico）+ UI 品牌色统一 | assets | 用户过目认可 |
| P9 | README（原理图 + 截图）、LICENSE、release CI、打 tag v0.1.0 | GitHub 首个 release | release 页可下载 zip，解压即用 |
| P10 | 真机全流程验收（清单见下） | 验收记录 | 全项通过 |

**P10 验收清单**：
1. 安装 OPPO 互联 + Clash TUN 复现故障 → 程序自动检测到并修复；
2. 开机自启：登录后无窗口无 UAC，托盘图标出现，延迟后开始检测；
3. TUN 正常时不做任何动作（不打扰）；
4. 手动「立即修复」可用；
5. 原有 ICS 共享在脉冲后恢复；
6. 关窗进托盘、托盘单击显隐、退出无残留；
7. 深浅主题切换 + 跟随系统；
8. 连续修复 3 次失败后停止并告警；
9. 日志文件滚动正常；
10. 卸载干净（删除计划任务 + %APPDATA% 目录说明）。

## 15. 里程碑估算（供参考，非承诺）

P1–P4（核心逻辑）为第一阶段重点，P3 是最大不确定点；P5–P8（UI/托盘/自启）为第二阶段；P9–P10 收尾发布。整体一个功能完整的 v0.1.0 是完全可交付的规模。
